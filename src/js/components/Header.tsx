import * as React from "react";

const Footer = ({debugMode}) => {
    return (
        <header role="header">
            <h2>
                端末認証/お知らせ
            </h2>
        </header>
    );
};

export default Footer;
