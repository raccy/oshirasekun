import * as React from "react";

const Footer = ({title}) => {
    return (
        <header role="header">
            <h2>
                {title}
            </h2>
        </header>
    );
};

export default Footer;
